

@url = tfgrid.reservation.container.1
#unique id inside the reservation is an autoincrement
workload_id = (I)
#links to unique node on the TFGrid
nodeid = (I)
flist = (S)
hub_url = (S)
#env variables
environment = (dict)
#the process to start
entrypoint = (S)
#corex yes or no
interactive = true (B)
volumes = (LO) !tfgrid.reservation.container.mount.1
network = (O) !tfgrid.reservation.network.1
#where to send the statistics too
stats_aggregator = (LO) !tfgrid.reservation.statsaggregator.1
#id of threebot who is the farmer
farmer_tid = (I)



@url = tfgrid.reservation.container.mount.1
volume_id = (S)
mountpoint = (S)

@url = tfgrid.reservation.network.1
network_id = (S)

